[build]
builder = "nixpacks"

[deploy]
startCommand = "apt-get update -y && apt-get install -y libstdc++6 && /opt/venv/bin/alembic upgrade head && /opt/venv/bin/uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
